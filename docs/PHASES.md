# ShopForge — Estado de fases

> Leer este archivo al iniciar sesión de trabajo. Detalle de cada fase en REQUIREMENTS.md §5.
> Vara de calidad: **FinPulse** (NO NEGOCIABLE — ver PORTFOLIO_PROJECTS.md §2 "Quality gate").

| Fase | Alcance | Estado |
|------|---------|--------|
| F1 | Fundaciones backend (solución, Domain, EF Core + migración, docker SQL, seed catálogo) | ✅ |
| F2 | Auth (JWT + refresh + lockout + RBAC), seed usuarios demo + reseñas | ✅ |
| F3 | Catálogo y reseñas (listado con filtros/orden/paginación, detalle, reviews) | ✅ |
| F4 | Carrito, checkout, Stripe, órdenes transaccionales, admin + dashboard | ✅ |
| F5 | Frontend base (scaffold, tema Emberline, i18n, auth UI, shell, interceptores) | ✅ |
| F6 | Frontend features (home, catálogo, detalle, carrito, checkout, cuenta, admin, demo guiada) | ✅ |
| F7 | Pulido y entrega (/about, TECHNICAL, README, CI, deploy, E2E prod, card portfolio) | ✅ |

## Log
- 2026-06-15 · **F7 ✅** Pulido y entrega. **`/about`** bilingüe EN/ES a paridad FinPulse/FleetGo (lead+chips+credenciales demo, Alcance, Arquitectura+bullets, tabla de patrones, Auth/seguridad, Integridad de órdenes, Catálogo/reseñas, Checkout/pagos, Performance, Testing, Trade-offs) — componente Angular plano con el design system Emberline, reemplaza el placeholder de la ruta. **`docs/TECHNICAL.md`** (deep-dive: arquitectura, commerce core con diagrama del checkout transaccional, OrderFinalizer, idempotencia, gateway strategy, seguridad, trade-offs). **README** reescrito con URLs de prod, capturas y credenciales (corrige Tailwind→SCSS y MediatR→service layer). Backend: **`UseForwardedHeaders`** para que el rate limiter vea la IP real tras el gateway de Azure. **Workflows**: `deploy-pages.yml` (inyecta `SHOPFORGE_API_BASE` + `--base-href /ShopForge/` + 404 SPA) y `keep-warm.yml` (ping con retry). **Deploy end-to-end**: Azure `shopforge-rg` (centralus) — App Service **F1** `shopforge-api-luisgxz` + SQL serverless free `shopforge-db-luisgxz/ShopForge` (auto-pause 60m, useFreeLimit); app settings con connection string/JWT/CORS/SeedDemoData; deploy zip (tar de Windows). `/health` 200, login demo 200, catálogo (16) sembrado. Repo público `LuisGxz/ShopForge` + GitHub Pages. E2E en prod + card en el portfolio. 61 tests verdes.
- 2026-06-14 · **F6 ✅** Frontend features completo. **Home** con grid de destacados en vivo. **Catálogo** (`/shop`) con filtros URL-driven (categoría/tueste/proceso/precio), búsqueda, orden, paginación server-side, rail de filtros responsive. **Detalle** (`/product/:slug`) con galería+thumbnails, selector de tamaño/molienda, qty stepper, add-to-bag, wishlist, reseñas paginadas + form de reseña (rating con estrellas, 409 ya-reseñado). **Carrito** con líneas editables, cupón (validación), resumen con envío/descuento y barra de envío gratis. **Carrito invitado** en localStorage que se fusiona al server al iniciar sesión (RF-04) — merge idempotente en el `effect` del store (construction-timing safe: el login renderiza fuera del shell). **Checkout** en 3 pasos (Envío→Pago→Confirmar) con `IPaymentGateway`: Stripe Elements cuando hay claves (carga dinámica de Stripe.js) o tarjeta de prueba presentacional con el stub; intent→confirm transaccional → **confirmación** con recibo. **Cuenta**: historial de órdenes (lista + detalle con snapshots y dirección) y **wishlist**. **Admin** (layout con tabs): dashboard (KPIs, gráfico de ventas, top productos, stock bajo, por estado), editor de productos (toggle activo/destacado + edición de variantes precio/stock) y bandeja de órdenes (filtro por estado, avanzar/cancelar). **Demo guiada**: tour de bienvenida (primera visita, persistido), guía flotante con pasos + cuentas demo + **role badge** (Invitado/Cliente/Admin). Sistema: `ToastService`+toasts, `StarsComponent` (half-star SVG), `ProductCardComponent` (quick-add + heart), `WishlistStore`, `CheckoutStore` (cupón + totales + handoff de orden), `provideAppInitializer` que restaura la sesión al arrancar (elimina el 401→refresh en vistas protegidas). i18n EN/ES extendido a todas las features. Build prod OK. **Verificación Playwright**: flujo de compra completo guest→login(merge)→checkout Stripe→confirmación→órdenes→wishlist + admin, responsive 390/768/1280, **0 errores de consola**. 20 capturas en `docs/screenshots/f6-*`. *Prerender SSG → se hace en F7 (deploy).*
- 2026-06-13 · Repo creado, requerimientos y arquitectura definidos (REQUIREMENTS.md).
- 2026-06-13 · **F1 ✅** Solución Clean Architecture (Domain/Application/Infrastructure/Api + tests), 13 entidades de dominio con lógica (stock, cupones, totales y transiciones de orden, lockout, merge de carrito), EF Core + configs + migración `InitialCreate`, docker-compose SQL Server, seed de catálogo realista (3 categorías, 16 productos, 42 variantes, 55 imágenes, 3 cupones). 25 tests de dominio verdes. Smoke test contra SQL Server local OK (migra + siembra + /health).
- 2026-06-13 · **F5 ✅** Frontend base — **Angular 20** (standalone + signals), SCSS propio (sin Tailwind, como FinPulse). Sistema de diseño **Emberline** (cream/bean/copper, DM Serif Display + Work Sans, tonos de gradiente para imágenes, botones/cards/chips/badges/skeletons, focus ring, reduced-motion). Core: config (API base inyectable), modelos TS, `ApiService` (todos los endpoints), `AuthService` (signals + refresh), `authInterceptor` (auto-refresh), `authGuard`/`adminGuard`, `LanguageService`+i18n EN/ES (copy a mano + mapas de enums), `api-error` (ProblemDetails → mensajes; status 0/5xx/429 honestos), `CartStore` (signals, badge). UI: shell (header sticky con logo/nav/carrito/idioma/menú usuario + footer, responsive con menú móvil), login (cuentas demo de un toque) + registro (errores legibles), home (hero del mockup + promos), placeholder elegante para rutas de F6 (sin enlaces muertos). Re-scaffold de Angular 18→20 por consistencia con los otros flagship. Build dev+prod OK. **Verificación visual Playwright en 390/768/1280 (home+login): 0 errores de consola**, capturas en `docs/screenshots/f5-*`.
- 2026-06-13 · **F4 ✅** Carrito + cupones + checkout + órdenes + admin. **Carrito** persistente por usuario (add/update/remove, merge, guard de stock). **Cupones** validación. **Checkout** con `IPaymentGateway` (Stripe real cuando hay claves, si no `StubPaymentGateway` determinista → la demo corre sin claves): `POST /checkout/intent` crea orden PendingPayment + PaymentIntent; `POST /checkout/confirm` verifica el pago server-side y **finaliza transaccionalmente** (decrementa stock, MarkPaid, redime cupón, vacía carrito) vía `OrderFinalizer`, idempotente; `POST /checkout/webhook` (Stripe firmado) como camino alterno. **Órdenes** del cliente (listado + detalle con snapshots). **Wishlist** toggle. **Admin** (policy Admin): productos (listado/actualizar producto/variante), órdenes (listado/avanzar fulfilment/cancelar con restock), dashboard de ventas (revenue, AOV, por estado, top productos, ventas/día, stock bajo). 61 tests verdes (13 nuevos de checkout/admin/cart). Smoke test E2E HTTP contra SQL Server: carrito→cupón→intent→confirm→orden Paid, carrito vaciado, stock 60→58, RBAC 403, dashboard, avance de estado, wishlist. **Envío**: gratis ≥ $60, si no $6.50.
- 2026-06-13 · **F3 ✅** Catálogo y reseñas (público): `GET /categories` (con conteo de productos activos), `GET /products` (búsqueda por nombre/notas/origen, filtros categoría/tueste/proceso/precio sobre la variante más barata, orden featured|price-asc|price-desc|rating|newest, paginación server-side), `GET /products/{slug}` (detalle con variantes ordenadas + galería + 404), `GET /products/{slug}/reviews` (paginado), `POST .../reviews` (auth, una por usuario→409, compra verificada vía órdenes, recálculo de rating). 48 tests verdes (14 nuevos). Smoke test HTTP OK: categorías, filtros, orden, búsqueda, detalle, 404, reseñas, empty state, publicar 201/409/401.
- 2026-06-13 · **F2 ✅** Auth completa: register/login/refresh rotativo/logout + JWT (claims sub/email/name/role) + lockout (5 intentos) + RBAC (policy `Admin`) + rate limiting `/auth` (10/min) + `/me`. Seed de usuarios demo (admin@shopforge.dev / demo@shopforge.dev + 10 reviewers) y **104 reseñas** con rating recalculado y consistente (1 producto sin reseñas para empty state). 34 tests verdes (9 de auth: register dup, login, fallo+lockout, mismo mensaje email/password, rotación de refresh, revocación, logout idempotente). Smoke test HTTP contra SQL Server local: login demo+admin 200, /me con JWT 200, /me sin token 401, refresh rota, register 200/409, validación 400, rate-limit 429. **Fix clave**: claves Guid → `ValueGeneratedNever` en `OnModelCreating` (sin esto, EF marca como Modified las entidades hijas añadidas vía navegación de un padre rastreado → UPDATE de 0 filas al sembrar reseñas).

## Cómo correr (dev)
- DB: SQL Server local (Windows auth, connection string en `appsettings.Development.json`) — alternativa: `docker compose up -d` y apuntar la connection string al puerto 14333.
- API migra y siembra catálogo demo automáticamente al arrancar en Development.
- `dotnet run --project backend/ShopForge.Api --urls http://localhost:5230`
- Tests: `dotnet test backend/ShopForge.sln`
- Demo (desde F2): admin@shopforge.dev / Admin1234! · demo@shopforge.dev / Demo1234!
